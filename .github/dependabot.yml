version: 2
updates:
  # Keep the pinned GitHub Actions SHAs up to date (opens PRs with the new SHA).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
